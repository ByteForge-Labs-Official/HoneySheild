# cAdvisor CLI options — bind-mount inside the container, see docker-compose.
--docker_only=true
--housekeeping_interval=15s
--whitelisted_container_labels=io.kubernetes.container.name,app,service
--disable_metrics=accelerator,cpu_topology,memory_numa
--enable_metrics=cpu_topology